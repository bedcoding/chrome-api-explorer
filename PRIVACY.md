# API Explorer 개인정보처리방침

최종 수정일: 2026-06-08

API Explorer 확장 프로그램(이하 "본 확장")은 사용자의 개인정보를 다음 원칙에 따라 처리합니다.

## 1. 수집하는 정보

본 확장은 사용자가 사이드패널을 열고 감지 기능을 켠 상태에서, 사용자가 방문한 웹페이지에서 발생하는 다음 정보만 수집합니다.

- GET HTTP 요청의 URL
- HTTP 메서드
- 요청이 발생한 페이지의 URL
- 사용자가 직접 입력한 메모, 즐겨찾기 표시, 도메인 화이트리스트 설정

요청 본문(body), 응답 내용, 쿠키, 인증 헤더 등은 일체 수집하지 않습니다.

## 2. 저장 위치

수집된 모든 데이터는 사용자 브라우저의 로컬 저장소(`chrome.storage.local`)에만 저장됩니다. 외부 서버로 전송되지 않으며, 개발자도 데이터에 접근할 수 없습니다.

## 3. 제3자 공유

본 확장은 어떠한 데이터도 제3자와 공유하지 않습니다. 분석 도구, 광고 네트워크, 외부 API에 데이터를 전송하지 않습니다.

## 4. 데이터 삭제

사용자는 다음 방법으로 수집된 데이터를 언제든 삭제할 수 있습니다.

- 확장 옵션 페이지에서 직접 삭제
- 확장 프로그램 제거 (모든 로컬 데이터가 함께 삭제됨)

## 5. 권한 사용 목적

| 권한 | 사용 목적 |
|---|---|
| `webRequest` | 페이지에서 발생하는 GET 네트워크 요청 감지 |
| `host_permissions` (`<all_urls>`) | 어느 페이지에서든 동작할 수 있도록 전체 호스트 접근 |
| `storage` | 수집한 API 목록, 메모, 설정값을 로컬에 저장 |
| `downloads` | JSON Export 시 결과 파일을 다운로드 폴더에 저장 |
| `tabs` | 현재 탭의 URL 정보 조회 (페이지별 그룹화에 사용) |
| `sidePanel` | 사이드패널 UI 표시 |

## 6. 변경 사항

본 방침이 변경될 경우, 변경일을 위에 명시합니다.

## 7. 문의

본 방침에 대한 문의는 GitHub Issues로 부탁드립니다.

- https://github.com/bedcoding/chrome-api-explorer/issues
